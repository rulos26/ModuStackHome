# ModuStackHome

ModuStack — Starter kit usuarios y correo (evaluar fusión con ModuStackAdmin)

**Repositorio:** https://github.com/rulos26/ModuStackHome


## Docker

```bash
docker compose up --build
```

App: http://localhost:8094

En Laravel, el .env debe usar `DB_HOST=mysql` y `DB_PORT=3306` cuando corre en Docker.
